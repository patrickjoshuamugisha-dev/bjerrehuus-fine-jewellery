# Bjerrehuus Visual QA Tracker

Use the HTML tracker instead:

`docs/current/VISUAL-QA-TRACKER.html`

The HTML version is the active visual QA file. This Markdown file is kept only as a pointer so old references do not break.
